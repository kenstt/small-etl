// Application layer: orchestrates use cases and entrypoints (CLI/Lambda) and pipelines
// Skeleton only for PR1 — no behavior yet.

pub mod cli {
    // Placeholder for CLI entrypoint and command assembly
    // e.g., pub mod main; (to be moved from src/main.rs in later PR)
}

pub mod lambda {
    // Placeholder for Lambda entrypoint
    // e.g., pub mod main; (to be moved from src/lambda.rs in later PR)
}

pub mod pipelines;
