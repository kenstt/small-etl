// Adapters layer: concrete implementations for external systems (config, storage, http, etc.)
// PR1 skeleton only; real implementations remain under existing src/config and others until later PRs.

pub mod config {
    // Placeholder for configuration providers (cli, lambda, toml, sequence)
}

pub mod storage {
    // Placeholder for storage backends (local, s3)
}

pub mod http {
    // Placeholder for http client wrappers
}
